# 学院级公开条目粒度

## 决策

日常新增的每一条公开招生信息必须对应一个官方明确可投的具体单位。`name` 保持学校名称，`institute` 必须为该单位的完整官方名称；学院、研究院、国际学院和联合培养单位均按此规则处理。

校级/研究生院的总通知可以作为发现入口和字段证据，但不能单独形成“全院系”“全校”或未指明单位的公开条目。扫描器必须继续展开公告中的学院目录、报名系统、附件和子页面，并为每个明确可投的单位分别生成条目。

## 失败语义

若官方页面只给出校级通知且不能从其正文、附件、系统或官方子页确认可投单位，则该线索保留在本轮私有重试队列，不公开泛校级条目；不得猜测、补全或复制学院名单。

同一官方通知明确覆盖多个单位时，每个单位独立成为一条新增项，并各自保留对应的 `institute`、稳定身份与可追溯官方证据。

## 兼容性

追加式发布不改写父快照。因此已发布的历史泛校级条目保持原样；其拆分、修正或合并只能在用户明确授权的独立维护任务中完成。

本次变更只更新项目运行规则，不改变公开 schema 或批准器。后续若要把该规则提升为机器门禁，应单独增加对 `institute` 的学院级身份校验及回归测试，且不得影响历史父项。

## 验收

- 项目 `AGENTS.md` 明确规定上述条目粒度、拆分路径和失败语义。
- 规则与“父快照只增不减”及深扫描/官方证据要求不冲突。
- 文档变更通过格式与差异检查。
